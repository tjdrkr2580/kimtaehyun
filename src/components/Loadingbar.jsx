import React from "react";

const Loadingbar = () => {
  return (
    <div class="progress-bar">
      <span class="bar">
        <span class="progress"></span>
      </span>
    </div>
  );
};

export default Loadingbar;
